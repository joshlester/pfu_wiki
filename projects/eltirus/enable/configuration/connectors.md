
# Connection Configuration
1. Add new connector credentials using the endpoint ```[POST] /v1/credentials```
2. Create interfaces for data e.g. response data
3. Create services to fetch and save data.