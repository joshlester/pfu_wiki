
## Build script
The dev and build scripts are located in the package.json

Edit function ```rendivo.window.loadTestView``` in ```/src/CustomerManager.tsx``` to change test view when debugging locally.
Debug runtime can be accessed at ```localhost:8080```
